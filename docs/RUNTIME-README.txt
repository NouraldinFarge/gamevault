GameVault embeds its React interface and Rust application code in GameVault.exe.
It uses the Microsoft Edge WebView2 Evergreen Runtime supplied by Windows.
A future release may bundle a fixed runtime if size and licensing tradeoffs
justify it.

